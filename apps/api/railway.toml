[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "npm ci && npm run build -w apps/api -w packages/db -w packages/core"

[deploy]
startCommand = "npm run start:api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
