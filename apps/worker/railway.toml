[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "npm ci && npm run build -w apps/worker -w packages/db -w packages/core"

[deploy]
startCommand = "npm run start:worker"
restartPolicyType = "on_failure"
